local.properties
keystore.properties
# 서명 키는 절대 커밋하지 않는다. 확장자를 둘 다 막는 이유: keytool 은 -keystore 에
# 적은 이름 그대로 파일을 만들어서 .jks 로도 .keystore 로도 나올 수 있고, 한쪽만
# 막아두면 다른 쪽 이름으로 만든 순간 조용히 커밋된다.
*.jks
*.keystore
google-services.json
.gradle/
build/
.kotlin/
*.apk
.idea/
.superpowers/
