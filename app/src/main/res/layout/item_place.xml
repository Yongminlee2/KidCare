<?xml version="1.0" encoding="utf-8"?>
<!-- 장소 한 줄. 왼쪽 글자 뭉치를 누르면 고치기, 오른쪽 끝은 삭제다.
     item_schedule.xml 과 같은 모양·같은 이유(카드로 줄의 경계를 눈에 보이게 한다)인데
     켬/끔 스위치가 없다. 장소의 '끔'은 알림 스위치 둘을 다 끄는 것이고 그건 편집 판
     안에 있다 — 줄에 스위치를 하나 더 두면 그게 도착인지 이탈인지 알 수 없다. -->
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="12dp"
    android:layout_marginVertical="5dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:baselineAligned="false"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingStart="12dp"
        android:paddingEnd="4dp"
        android:paddingVertical="6dp">

        <LinearLayout
            android:id="@+id/row_body"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:background="?attr/selectableItemBackground"
            android:clickable="true"
            android:focusable="true"
            android:minHeight="48dp"
            android:orientation="vertical"
            android:paddingHorizontal="4dp"
            android:paddingVertical="8dp">

            <TextView
                android:id="@+id/title_text"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textAppearance="?attr/textAppearanceBodyLarge" />

            <TextView
                android:id="@+id/detail_text"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textAppearance="?attr/textAppearanceBodySmall"
                android:textColor="?attr/colorOnSurfaceVariant" />
        </LinearLayout>

        <com.google.android.material.button.MaterialButton
            android:id="@+id/delete_button"
            style="@style/Widget.KidCare.Button.Text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:insetLeft="0dp"
            android:insetRight="0dp"
            android:minWidth="0dp"
            android:paddingHorizontal="8dp"
            android:text="@string/place_delete"
            android:textColor="?attr/colorError"
            app:iconPadding="0dp" />
    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
